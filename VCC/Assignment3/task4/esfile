FROM debian:bookworm
# Copy the external-service directory into /opt/external-service
COPY external-service/ /opt/
# Run commands to update packages and install Python3, Flask, and Requests
RUN apt update && apt install -y python3 python3-flask python3-requests && apt-get clean
